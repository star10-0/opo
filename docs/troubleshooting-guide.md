# دليل استكشاف المشاكل الشائعة (Phase 11)

## 1) لا تظهر البيانات في اللوحة
**الأعراض:** بطاقات فارغة أو رسالة عدم وجود بيانات.

**التحقق والحل:**
1. تأكد من اختيار محطة صحيحة.
2. تحقق أن اليوم التشغيلي مفتوح.
3. افحص backend health:
   ```bash
   cd backend
   npm run healthcheck
   ```
4. راجع صلاحيات المستخدم (قد لا يملك `view_dashboard` أو `view_reports`).

---

## 2) لا يمكن اختيار محطة
**الأعراض:** قائمة المحطات فارغة أو لا تحفظ الاختيار.

**التحقق والحل:**
1. تأكد أن المستخدم مرتبط بمحطة واحدة على الأقل.
2. إذا كانت أول مرة والنظام بلا محطات:
   - يجب على المدير إنشاء أول محطة.
3. امسح بيانات المتصفح المحلية ثم أعد تسجيل الدخول.
4. تحقق من استجابة endpoint المحطات المسموح بها.

---

## 3) backend لا يعمل
**الأعراض:** فشل طلبات API أو رسائل اتصال مرفوض.

**التحقق والحل:**
1. تأكد من وجود ملف `backend/.env` بالقيم الصحيحة.
2. تحقق من `MONGO_URI`.
3. شغّل backend وتابع الأخطاء:
   ```bash
   cd backend
   npm run dev
   ```
4. افحص الصحة:
   ```bash
   cd backend
   npm run healthcheck
   ```

---

## 4) frontend لا يظهر التحديثات
**الأعراض:** تم الحفظ لكن الواجهة لا تتغير فورًا.

**التحقق والحل:**
1. حدّث الصفحة (Hard Refresh).
2. تأكد أن backend أعاد استجابة نجاح.
3. تحقق من `VITE_API_BASE_URL` أنه يشير للـ backend الصحيح.
4. أعد تشغيل frontend:
   ```bash
   cd fuel-frontend-v2
   npm run dev
   ```

---

## 5) لا تظهر التقارير
**الأعراض:** تبويب التقارير فارغ أو خطأ تحميل.

**التحقق والحل:**
1. تأكد من صلاحية `view_reports`.
2. تأكد من اختيار المحطة.
3. جرب نطاق تاريخ يحتوي بيانات فعلية.
4. تحقق من عدم وجود تعليق/أرشفة تمنع احتساب بعض النتائج.

---

## 6) لا يمكن الاعتماد أو الحفظ
**الأعراض:** زر الحفظ/الاعتماد لا يعمل أو يظهر رفض.

**التحقق والحل:**
1. تحقق من اكتمال الحقول الإلزامية.
2. تحقق من الصلاحيات (مثلاً اعتماد الإغلاق للمحاسب فقط غالبًا).
3. إذا السجل مؤرشف أو حساس:
   - يجب استخدام Approval Request بدل تعديل مباشر.
4. راجع رسائل الخطأ المعروضة في الواجهة.

---

## 7) مشاكل اللغة أو الصلاحيات
**الأعراض:** ظهور نصوص غير متوقعة أو اختفاء أزرار مهمة.

**التحقق والحل:**
1. اختر اللغة من أعلى الواجهة (العربية/الإنجليزية).
2. تأكد من الدور المخزن بعد تسجيل الدخول.
3. تأكد أن حساب المستخدم يملك الأذونات الصحيحة للمحطة.
4. سجّل خروج ثم دخول لإعادة تحميل الصلاحيات.

---

## 8) متى يتم التصعيد؟
صعّد المشكلة إلى فريق التطوير عند:
- فشل متكرر في `/api/health`.
- أخطاء قاعدة بيانات متكررة.
- اختلاف واضح بين أرقام التقارير والبيانات المصدرية.
- عدم قدرة أي دور أساسي على إكمال المسار اليومي.
